# 献立管理アプリ 仕様書

夫婦2人で使う、レシピ管理＋献立カレンダー＋買い物リストのWebアプリ。
iPhoneのブラウザとPCブラウザの両方から利用する。ネイティブアプリ化はしない（PWA対応）。

---

## 1. 技術構成

| 領域 | 採用技術 | 備考 |
|---|---|---|
| フレームワーク | Next.js (App Router) + TypeScript | |
| UI | Tailwind CSS | モバイルファーストで設計 |
| ホスティング | Vercel（Hobbyプラン・無料） | 個人利用のため商用制限に抵触しない |
| DB | Supabase Postgres（無料枠） | |
| 画像ストレージ | Supabase Storage（無料枠） | 料理写真・取り込み写真 |
| AI | Gemini API `gemini-3.5-flash`（無料枠） | 15 RPM / 1,500 RPD。夫婦2人の利用なら十分 |
| 動画メタデータ | YouTube Data API v3（無料枠） | `videos.list` は1リクエスト1ユニット、1日1万ユニット |

### 重要な制約と方針

- **Vercelのサーバーレス関数は実行10秒以内**。Gemini呼び出しが長引く場合を想定し、取り込み処理は「①メタデータ取得 → ②AI解析」の2リクエストに分割する。
- **Supabaseへのアクセスはすべてサーバーサイド（API Routes / Server Actions）経由**にする。service role keyをクライアントに露出させない。
- **Gemini無料枠は送信データがモデル改善に利用される可能性がある**。レシピ情報のみを送る設計とし、個人情報は送らない。
- **取り込み時に元テキスト・サムネイル画像を必ず自前DBに保存する**。元動画が削除・非公開になってもレシピが失われないようにするため。

---

## 2. 認証

- **夫婦で共有するパスワード1つ**による簡易認証。
- パスワードは環境変数（`APP_PASSWORD`）に保持。ハッシュ化して比較する。
- 認証成功時に httpOnly / Secure / SameSite=Lax のセッションCookieを発行。有効期限は長め（90日程度）に設定し、スマホで毎回ログインする手間を避ける。
- 未認証時はすべてのページとAPIを `/login` にリダイレクト。
- ユーザーの区別は行わない（データはすべて共有）。ただし感想の記録には後述の「記入者」欄を持たせる。

---

## 3. データモデル

```
recipes
  id            uuid PK
  title         text
  source_type   text      -- 'youtube' | 'instagram' | 'photo' | 'manual'
  source_url    text      -- 元URL（photo/manualの場合はnull）
  thumbnail_url text      -- サムネイル（Supabase Storageに保存したもの）
  raw_text      text      -- 取り込み元の生テキスト（概要欄・OCR結果など）
  base_servings numeric   -- 元レシピの人数（例: 2）
  ingredients   jsonb     -- [{name, amount, unit, is_pantry}]
  steps         jsonb     -- [{order, text}]  ※常に base_servings 基準の原文
  cook_time_min integer
  tags          text[]
  memo          text
  is_planned    boolean   -- 「作りたいリスト」フラグ
  created_at    timestamptz
  updated_at    timestamptz

recipe_scaled_cache      -- 人数変更時のAI生成結果をキャッシュ
  id            uuid PK
  recipe_id     uuid FK
  servings      numeric
  ingredients   jsonb
  steps         jsonb
  created_at    timestamptz
  UNIQUE(recipe_id, servings)

menu_entries             -- 献立カレンダー
  id            uuid PK
  date          date
  meal_type     text      -- 現状は 'dinner' 固定。将来の朝昼拡張用に持たせる
  recipe_id     uuid FK
  servings      numeric   -- その日の人数（未指定なら base_servings）
  position      integer   -- 同日に複数品置く場合の並び順
  note          text

cooking_logs             -- 調理後の感想（調理するたびに1件積む）
  id            uuid PK
  recipe_id     uuid FK
  menu_entry_id uuid FK nullable
  cooked_on     date
  rating        integer   -- 1〜5
  comment       text
  photo_url     text      -- 完成写真
  author        text      -- 'me' | 'partner' など任意ラベル
  created_at    timestamptz

shopping_lists
  id            uuid PK
  start_date    date
  end_date      date
  created_at    timestamptz

shopping_items
  id            uuid PK
  list_id       uuid FK
  name          text
  amount        numeric nullable
  unit          text nullable
  category      text      -- '野菜' | '肉' | '魚' | '乳製品' | '調味料' | 'その他'
  checked       boolean
  source_recipe_ids uuid[]
  is_manual     boolean   -- 手動追加した品かどうか
```

---

## 4. 機能仕様

### 4.1 レシピ取り込み（YouTube）

1. URLを入力欄に貼り付ける。
2. YouTube Data API v3 の `videos.list`（part=snippet）でタイトル・概要欄・サムネイルURLを取得。
3. サムネイル画像をダウンロードし Supabase Storage に保存（元動画削除への保険）。
4. 概要欄テキストをGeminiに渡し、材料と手順に構造化（後述のプロンプト）。
5. 抽出結果を**編集可能なフォームに表示**してから保存する。AIの抽出は完璧ではないため、必ず人間が確認・修正できる画面を挟む。

概要欄にレシピが書かれていない場合は、字幕取得へフォールバックする（字幕が取得できなければ、後述の手動貼り付けへ）。

### 4.2 レシピ取り込み（Instagram）

Instagramは公式APIで一般投稿の本文を安定して取得する手段がないため、**自動取得は行わない前提で設計する**。

- URLを入力すると、表示用に投稿を埋め込む（公式のembedスクリプト）。
- **キャプションを手動で貼り付けるテキストエリアを必ず用意する**。貼り付けたテキストをGeminiに渡して構造化する。
- 自動取得を試みる実装は入れない（不安定・規約グレーのため）。

### 4.3 レシピ取り込み（写真）

主用途は**料理本のページ**（活字・整った紙面）と想定し、まずここでの精度を優先する。手書きメモやレシピカードは対応対象に含めるが、精度は活字ほど出ない前提で扱う（優先度: YouTube取り込み > 写真取り込み）。

- 写真を1〜複数枚アップロード。
- Geminiのマルチモーダル機能に画像を直接渡し、材料と手順を抽出する（別途OCRライブラリは不要）。
- 複数枚の場合は1回のリクエストにまとめて渡し、1つのレシピとして構造化する（見開きページなどを想定）。
- 抽出結果は編集フォームで確認してから保存。
- アップロードした写真は Supabase Storage に保存し、レシピ詳細ページで閲覧できるようにする。

### 4.4 レシピ詳細ページ

- 材料リスト、手順、タグ、調理時間、メモを表示。
- **元コンテンツをページ内で再生できる**：
  - YouTube → iframe埋め込みプレイヤー
  - Instagram → 公式embed
  - 写真 → 画像ビューア（複数枚はスワイプ）
- 調理履歴（何回作ったか・最終調理日）を表示。
- 過去の感想ログを新しい順に一覧表示。

### 4.5 人数変更（分量スケーリング）

- 人数を「4人前」「4.5人前」のように小数含みで指定できる。
- **材料の分量は比例計算**で算出（AIを使わない。計算誤差を避けるため）。
  - 割り切れない場合は小数第1位まで表示（例: 卵1.5個）。
  - 表示の隣に元の分量を薄字で併記する（例: `卵 1.5個（元: 2人前で1個）`）。
- **手順文はGeminiで書き換える**。プロンプトで以下を厳守させる：
  - 分量に関する数字（大さじ・ml・g・個数）は新しい人数に合わせて書き換える
  - **加熱時間・冷蔵時間・温度は書き換えない**（人数に比例しないため）
  - 調理器具のサイズに言及がある場合は「フライパンが小さければ2回に分けて」等の注記を添えてよい
- **元の手順は常に確認できる**：詳細ページに「元の手順（◯人前）を表示」トグルを設置し、原文と書き換え版を切り替え表示できるようにする。原文（`recipes.steps`）は絶対に上書きしない。
- 一度生成したスケーリング結果は `recipe_scaled_cache` に保存し、同じ人数で再度開いたときはAPIを呼ばない（無料枠の節約）。

### 4.6 献立カレンダー

- **1日1枠（夕食のみ）**。DBには `meal_type` を持たせておき、将来の朝昼拡張に備える。
- 月表示（PC）とリスト表示（スマホ）を切り替えられるようにする。スマホの月カレンダーはセルが狭く操作しづらいため。
- レシピを日付にドラッグ＆ドロップで配置（PC）／タップして日付選択（スマホ）。
- 同じ日に複数品を置けるようにする（`position` で並び順管理）。副菜を追加したくなる場面があるため。
- 各エントリに人数を指定できる（来客時など）。

### 4.7 カレンダーのコピー

- 「◯月の献立を△月にコピー」する機能。
- **曜日基準でコピーする**（例: 7月の第1土曜 → 8月の第1土曜）。土日に手の込んだ料理を配置する運用と相性がよいため。日付基準も選べるようにオプションを用意する。
- コピー先にすでに献立がある日の扱いを選べるようにする：「上書き」／「スキップ」／「追加」。
- 期間指定コピー（例: 特定の1週間だけ別の週にコピー）も可能にする。

### 4.8 買い物リスト

- 期間を指定（例: 今日から1週間）して生成。
- 期間内の献立エントリから材料を集約する。
  - **同じ材料は合算する**（例: 玉ねぎ 1個 + 玉ねぎ 0.5個 → 玉ねぎ 1.5個）。表記ゆれ（玉ねぎ／たまねぎ／玉葱）はGeminiで名寄せする。
  - 単位が異なる場合（100g と 1本 など）は合算せず、両方を併記する。
- **調味料などの常備品はデフォルトで除外**。材料に `is_pantry` フラグを持たせ、「常備品も表示する」トグルで切り替えられるようにする。
- **売り場カテゴリ順にグルーピング**して表示（野菜 → 肉 → 魚 → 乳製品 → その他 → 調味料）。カテゴリ分類はGeminiに判定させる。
- チェックボックスで購入済みにできる。チェック状態はDBに保存するが、リアルタイム同期は不要（分担して買い物しないため）。画面を開き直したときに最新状態が反映されれば十分。
- 手動での品目追加ができる（牛乳、トイレットペーパーなど献立以外のもの）。
- リスト全体をテキストとしてコピーできるボタン（LINEやメモアプリに貼れるように）。

### 4.9 感想の記録

- 献立カレンダーの各エントリから「作った」を記録すると、感想入力画面が開く。
- 5段階評価、コメント、完成写真（任意）、記入者ラベル。
- **調理するたびに1件ずつ履歴として積む**（レシピに1つの固定コメント欄にはしない）。「前回は塩辛かったので今回は控えめに」といった改善の記録を残せるようにするため。
- レシピ詳細ページで履歴を時系列に表示。

### 4.10 タグと絞り込み

- タグは複数付与可能（`text[]`）。
- **初期タグを用意しつつ、自由入力も可能**にする。
  - 初期セット: 肉料理 / 魚料理 / 麺料理 / 丼もの / 汁物 / 副菜 / サラダ / 中華 / 和食 / 洋食 / 時短 / 作り置き
- レシピ一覧でタグの複合絞り込み（AND条件）ができる。
- タグに加えて、調理時間・評価・最終調理日でも絞り込めるようにする。

### 4.11 検索

- レシピ名・材料名の部分一致検索。

### 4.12 調理履歴とマンネリ防止

- 各レシピに「調理回数」と「最終調理日」を表示。
- レシピ一覧に「しばらく作っていない順」の並び替えを用意。献立決めで最も困るのがマンネリのため、この導線は目立つ位置に置く。

### 4.13 AI献立提案（あると便利）

- 「1週間分の献立を提案」ボタン。
- 登録済みレシピの一覧（タイトル・タグ・最終調理日）をGeminiに渡し、以下の条件で組ませる：
  - 肉料理と魚料理が偏らないようにする
  - 直近1ヶ月に作ったレシピは避ける
  - 平日は調理時間が短いもの、週末は手の込んだものを優先
- 提案結果は**下書きとしてカレンダーに仮配置**し、確定前に手で差し替えられるようにする。

### 4.14 作りたいリスト

- 取り込んだがまだカレンダーに配置していないレシピの置き場（`is_planned` フラグ）。
- カレンダーに配置するとフラグが外れる。

### 4.15 データエクスポート

- 全レシピ・献立・感想をJSONでダウンロードできる。
- 無料枠の仕様変更やサービス終了に備えた保険。

---

## 5. Geminiプロンプト設計

すべてのプロンプトで **JSONのみを返させる**（前置き・Markdownのコードフェンス禁止）。レスポンスは念のためフェンス除去してからパースし、失敗時はリトライ1回、それでも失敗したら手動入力画面に誘導する。

### 5.1 レシピ抽出（テキスト・画像共通）

出力スキーマ：

```json
{
  "title": "料理名",
  "base_servings": 2,
  "cook_time_min": 30,
  "ingredients": [
    { "name": "玉ねぎ", "amount": 1, "unit": "個", "is_pantry": false },
    { "name": "醤油", "amount": 2, "unit": "大さじ", "is_pantry": true }
  ],
  "steps": [
    { "order": 1, "text": "玉ねぎを薄切りにする" }
  ],
  "tags": ["肉料理", "時短"],
  "confidence": "high"
}
```

指示に含める要点：
- 元テキストに書かれていない材料や手順を創作しない
- 人数が明記されていない場合は `base_servings` を `null` にする（推測しない）
- 砂糖・塩・醤油・味噌・油・酒・みりん・こしょう等の基本調味料は `is_pantry: true`
- 情報が不足している場合は `confidence` を `"low"` にする（UIで警告表示に使う）

### 5.2 手順の人数変更

- 入力：元の手順配列、元の人数、新しい人数
- 出力：書き換え後の手順配列（同じスキーマ）
- 厳守事項として「加熱時間・温度・冷蔵時間は変更しない」を明示する

### 5.3 買い物リストの名寄せとカテゴリ分類

- 入力：材料名の配列
- 出力：`[{ "original": "たまねぎ", "normalized": "玉ねぎ", "category": "野菜" }]`

---

## 6. UI要件

- **モバイルファースト**。iPhoneのSafariでの操作を最優先で設計する。
- **PWA対応**：`manifest.json`、`apple-touch-icon`、`display: standalone` を設定し、ホーム画面に追加するとアプリのように起動できるようにする。
- タップ領域は十分に大きく取る（キッチンで濡れた手・片手操作を想定）。
- レシピ詳細（調理中に見る画面）は**画面が自動で暗くならないようにする**（Screen Wake Lock API。iOS Safariの対応状況を確認のうえ、非対応ならフォールバックなしで可）。
- 調理中は材料と手順を大きめの文字で表示する「調理モード」を用意する。
- ダークモード対応。

---

## 7. 実装フェーズ

**フェーズ1（MVP）**
1. 認証（共有パスワード）
2. レシピの手動登録・一覧・詳細
3. 献立カレンダー（配置・削除）
4. タグと絞り込み

**フェーズ2（取り込み）**
5. YouTube取り込み
6. 写真取り込み
7. Instagram（embed表示＋キャプション貼り付け）

**フェーズ3（運用機能）**
8. 買い物リスト生成
9. 人数変更（スケーリング）
10. 感想記録
11. カレンダーコピー

**フェーズ4（拡張）**
12. 調理履歴・マンネリ防止の並び替え
13. AI献立提案
14. エクスポート

---

## 8. 未確定・実装時に判断する項目

- Screen Wake Lock API のiOS Safari対応可否
- Supabase無料枠のストレージ容量に対する写真の保存方針（アップロード時にリサイズ・圧縮する前提でよいか）
- Gemini無料枠のレート上限は変動するため、実装時にGoogle AI Studioのレート制限画面で最新値を確認する
- YouTube字幕取得の実装方式（公式APIでの字幕取得は権限が必要なため、概要欄のみで運用する判断もあり得る）
